<!DOCTYPE html>
<html lang="no">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>A-to-Z IT Solutions | Oslo, Norge</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #38bdf8;
            --bg-dark: #0f172a;
            --card-bg: #1e293b;
            --text-main: #f1f5f9;
        }

        body { 
            font-family: 'Inter', sans-serif; 
            background: var(--bg-dark); 
            color: var(--text-main); 
            margin: 0; 
            padding: 0; 
            line-height: 1.6;
        }

        /* منطقة الشعار - Header */
        header {
            padding: 50px 20px;
            background: radial-gradient(circle at center, #1e293b 0%, #0f172a 100%);
            text-align: center;
        }

        .logo-wrapper {
            max-width: 350px; /* حجم الشعار المثالي ليكون واضح ومش ضخم */
            margin: 0 auto;
            filter: drop-shadow(0 10px 20px rgba(0,0,0,0.3)); /* ظل خفيف للشعار */
        }

        .logo-wrapper img {
            width: 100%;
            height: auto;
            display: block;
        }

        /* Hero Section */
        .hero { 
            padding: 20px 20px 60px;
            text-align: center;
        }

        h1 { 
            font-size: 2.8em; 
            margin: 10px 0; 
            background: linear-gradient(to right, #fff, var(--primary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            font-weight: 800;
        }

        .subtitle { 
            color: #94a3b8; 
            max-width: 700px; 
            margin: 0 auto 30px; 
            font-size: 1.1em;
        }

        .btn { 
            background: var(--primary); 
            color: #0f172a; 
            padding: 16px 32px; 
            text-decoration: none; 
            border-radius: 12px; 
            font-weight: bold; 
            transition: 0.3s ease;
            display: inline-block;
        }

        .btn:hover { 
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(56, 189, 248, 0.3);
        }

        /* شبكة الخدمات */
        .services { 
            display: grid; 
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); 
            gap: 25px; 
            max-width: 1100px; 
            margin: 40px auto; 
            padding: 0 20px;
        }

        .card { 
            background: var(--card-bg); 
            padding: 40px; 
            border-radius: 20px; 
            border: 1px solid rgba(255,255,255,0.05);
            text-align: left;
            transition: 0.3s;
        }

        .card:hover { 
            border-color: var(--primary);
            transform: scale(1.02);
        }

        .card h3 { color: var(--primary); margin-top: 0; font-size: 1.4em; }
        .en-title { font-size: 0.8em; color: #64748b; text-transform: uppercase; letter-spacing: 1px; }

        /* Footer */
        footer { 
            padding: 60px 20px; 
            background: #020617; 
            border-top: 1px solid #1e293b;
            margin-top: 60px;
        }

        .org-nr { 
            display: inline-block;
            background: #1e293b;
            padding: 5px 15px;
            border-radius: 30px;
            font-size: 0.85em;
            color: var(--primary);
            margin-top: 15px;
        }
    </style>
</head>
<body>

    <header>
        <div class="logo-wrapper">
            <img src="logo.png" alt="A-to-Z IT Solutions">
        </div>
    </header>

    <div class="hero">
        <h1>Secure. Scalable. Smart.</h1>
        <p class="subtitle">Premium IT Solutions in Oslo. We specialize in high-performance software and secure infrastructure.</p>
        <a href="mailto:a-to-z@a-to-z.no" class="btn">Contact Us Now</a>
    </div>

    <main class="services">
        <div class="card">
            <span class="en-title">Development</span>
            <h3>Utvikling & Programmering</h3>
            <p>Custom software engineering with a focus on C, Java, and robust system architecture.</p>
        </div>

        <div class="card">
            <span class="en-title">Security</span>
            <h3>IT-Konsulenttjenester</h3>
            <p>Protecting your assets through ethical hacking, security audits, and expert consulting.</p>
        </div>

        <div class="card">
            <span class="en-title">Operations</span>
            <h3>Drift & Administrasjon</h3>
            <p>Managed Linux services and cloud administration ensuring 24/7 reliability for your business.</p>
        </div>
    </main>

    <footer>
        <p><strong>AHMED ALI Enkeltpersonforetak</strong></p>
        <p>Oslo, Norge | <a href="mailto:a-to-z@a-to-z.no" style="color:var(--primary); text-decoration:none;">a-to-z@a-to-z.no</a></p>
        <span class="org-nr">Org.nr: 935 048 362</span>
    </footer>

</body>
</html>
